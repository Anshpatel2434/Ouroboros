---
title: "TypeScript 4.9 / Next.js stack playbook"
source_url: ""
publisher: Ouroboros gap research
retrieved: 2026-09-07
domain: stack-playbooks
doc_type: reference
relevance: "Verification commands and minimal skeleton for TypeScript 4.9 / Next.js, researched because the corpus had no coverage when a project requested this stack."
---

## Summary

Generated by Ouroboros gap research when a developer specified TypeScript 4.9 / Next.js and the
corpus had no playbook for it. It records the canonical verification commands
and the minimal project layout the Generator needs to emit a repository whose
`verify.sh` passes before any feature exists.

Machine-derived rather than sourced from a single document: treat the commands
as authoritative for generation, and correct this file if a real project on this
stack proves any of them wrong.

## Key knowledge

**Package manager:** `npm`

**Verification commands**

- `install`: `npm install`
- `test`: `npm test`
- `lint`: `npm run lint`
- `typecheck`: `npm run typecheck`
- `build`: `npm run build`
- `smoke`: `npm run start`

**Layout**

- The project follows the standard Next.js structure with pages and components.

**Skeleton files**

- `package.json` — Defines project metadata and dependencies.

**Gotchas**

- Ensure you have Node.js installed (version 14.0 or later is recommended for Next.js).
- Run 'npm install' to install dependencies before running any other commands.
- The default test command is a placeholder; you may want to set up a testing framework like Jest for actual tests.

**Sources consulted**

- None recorded.

## Notable quotes

None — this document is machine-generated rather than sourced from prose.

## Application to Ouroboros

The Generator reads this playbook to emit `verify.sh`, `init.sh`, and the
project skeleton for this stack. Its presence is also what flips
`stack.corpus_covered` to true, which is what lets the ambiguity lint stop
refusing generation for this stack.
